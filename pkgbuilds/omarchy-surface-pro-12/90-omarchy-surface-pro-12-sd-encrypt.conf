# mkinitcpio 41 concatenates /etc/mkinitcpio.conf then lexically sorted
# /etc/mkinitcpio.conf.d/*.conf. Its ARM default uses the systemd hook, so use
# sd-encrypt (not classic encrypt) and retain keyboard/sd-vconsole from base.
HOOKS=(${HOOKS[@]/filesystems/sd-encrypt filesystems})
